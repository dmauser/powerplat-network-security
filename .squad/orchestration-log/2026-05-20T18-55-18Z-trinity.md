# Orchestration Log — Trinity

**Agent:** Trinity (Infra/Bicep)  
**Timestamp:** 2026-05-20T18:55:18Z  
**Request:** Squad repo spot review, errors & improvements  
**Mode:** Parallel fan-out (5 agents)

---

## Scope

**Primary:** Bicep review, IaC validation, build/lint status, deployment script health  
**Files reviewed:** infra/main.bicep, infra/modules/*, scripts/01-deploy.sh, .gitattributes

---

## Work Completed

### 1. Bicep Build & Lint Validation

**Status:** ✅ PASS

- `bicep build infra/main.bicep` — Success
- `bicep lint infra/main.bicep` — Success
- All modules under `infra/modules/` — Build & lint pass

### 2. Deployment Script Fix

**File:** `scripts/01-deploy.sh`  
**Issue:** Checked out with CRLF line endings; caused `bash -n scripts/01-deploy.sh` to fail on function definitions

**Fix:** Normalized to LF line endings  
**Related:** Added `*.sh text eol=lf` to `.gitattributes` to enforce LF for future shell scripts

### 3. Architecture Verification

**Verified:** Active IaC matches documented lab shape:
- ✅ eastus + westus VNets
- ✅ `snet-pp-delegated` delegated to `Microsoft.PowerPlatform/enterprisePolicies`
- ✅ Private DNS zones linked to both VNets
- ✅ Private endpoints in eastus `snet-pep` (per architecture)
- ✅ UAMI outputs stable (`guid(...)` role assignments)
- ✅ `NetworkInjection` enterprise policy referencing both VNets' delegated subnet names
- ✅ `scripts/01-deploy.sh` targets subscription-scope deployment
- ✅ Outputs consumed by follow-on scripts correctly

---

## Findings & Filings

### 🟡 Shared-Resource Location Drift

**Scope:** `infra/main.bicep`, `infra/parameters/dev.parameters.json`

**Finding:** Shared resources (RG, KV, Storage, SQL) default to `westus3`, while VNets stay `eastus+westus`.

**Decision needed:**
- Align `defaultLocation` to paired region, OR
- Codify and justify why shared services live outside paired network regions

**Filed:** Included in Morpheus findings; Trinity owns follow-up

### 🟡 Key Vault & Storage AzureServices Bypass

**Scope:** `infra/modules/keyvault.bicep`, `infra/modules/storage.bicep`

**Finding:** Both modules declare `networkAcls.bypass = 'AzureServices'` despite disabling public network access. This weakens the private-only story unless intentionally justified.

**Decision needed:**
- Test whether bypass can be removed cleanly
- If not removable, document exact product requirement

**Filed:** Included in Morpheus findings; Trinity owns follow-up

### 🟡 Infra-Only Provider Registration Ownership

**Scope:** `scripts/01-deploy.sh` vs. `infra/` module structure

**Finding:** User-assigned managed identity deployed, but `Microsoft.ManagedIdentity` provider registration is outside Trinity's module scope. Tank narrowed `scripts/00-prereqs.sh` to Power Platform VNet prerequisites only.

**Decision needed:** Should `scripts/01-deploy.sh` self-register `Microsoft.ManagedIdentity` and other infra-only providers, or should requirement be documented elsewhere?

**Filed:** `.squad/decisions/inbox/tank-trinity-managedidentity-provider-ownership.md`

---

## Work Artifacts

**Files updated:**
- `scripts/01-deploy.sh` (CRLF → LF)
- `.gitattributes` (added shell script LF enforcement)

**Files reviewed:** infra/main.bicep + 6 modules, scripts/01-deploy.sh  
**Build/lint passes:** 100% (all Bicep)

---

## Status

✅ **Sweep complete**  
No architectural refactor applied. No edits to archive/, PowerShell scripts, docs, or network validation script.

**Next steps:**
- Decision on shared-resource location (Morpheus + Trinity)
- AzureServices bypass testing + justification
- Provider registration ownership (Tank + Trinity)
