# Orchestration Log — Neo

**Agent:** Neo (Validator)  
**Timestamp:** 2026-05-20T18:55:18Z  
**Request:** Squad repo spot review, errors & improvements  
**Mode:** Parallel fan-out (5 agents)

---

## Scope

**Primary:** Validation script rewrite, demo script clarity, connector-specific test coverage  
**Files reviewed:** scripts/03-validate-network.sh, docs/demo-script.md, docs/connectors/*.md  
**Files updated:** scripts/03-validate-network.sh, docs/demo-script.md

---

## Work Completed

### 1. Network Validation Script Rewritten (scripts/03-validate-network.sh)

**Improvements:**

- **Private DNS validation:** Compare each Private DNS A record to corresponding private endpoint IP (stronger than dig-based operator-box tests)
- **Zone link verification:** Explicitly verify private DNS zones are linked to both VNets containing `snet-pp-delegated`
- **Explicit deny-path checks:**
  - Key Vault: Expect `403`
  - SQL: Expect TCP 1433 blocked
  - Blob anonymous: Expect `403`
  - Blob SAS-over-public: Expect `403`
- **Scope clarity:** Added explicit statement that bash validator cannot execute inside `snet-pp-delegated` (true runtime is Managed Environment / connector execution). Script validates Azure-side DNS plumbing and denial paths; allow-path checks are doc-based Managed Environment runs.
- **Removed loose checks:** Replaced `non-2xx` HTTP checks with explicit status codes

**Benefits:** Stronger validation of actual Private Link setup while being honest about scope limits

### 2. Demo Script Rewritten (docs/demo-script.md)

**Improvements:**

- **Added dated pre-demo validation commands** for repeatable setup checks
- **Separated workstation denial probes from Managed Environment allow-path proof**
- **Made negative-path tests explicit** for Key Vault, SQL, and Blob
- **Emphasized:** Workstation tests prove denial; Managed Environment runs prove the allow path (dual-path proof pattern)
- **Clearer section organization** for demo flow

### 3. Connector Test Coverage (Neo → Niobe merge pipeline)

**Status:** Neo prepared 4 merge-ready test-step files for Niobe to integrate into connector docs:

1. **neo-tests-keyvault.md** — Positive/negative tests for Key Vault connector
   - Allow path: Flow in Managed Environment succeeds, returns secret value
   - Deny path: Flow in non-linked environment fails (403)
   - Workstation: curl to public endpoint returns 403
   - Azure-side: Verify Private DNS A record + zone link

2. **neo-tests-sql.md** — Positive/negative tests for SQL connector
   - Allow path: Flow returns rows from `dbo.Sales` in Managed Environment
   - Deny path: Flow from non-linked environment times out or fails
   - Workstation: TCP 1433 test fails
   - Azure-side: Verify Private DNS A record + zone link

3. **neo-tests-blob.md** — Positive/negative tests for Blob connector
   - Allow path: Flow returns content from `demo/hello.txt` in Managed Environment
   - Deny paths: Anonymous blob request (403), SAS-over-public URL (403)
   - Azure-side: Verify Private DNS A record + zone link

4. **neo-tests-custom-http.md** — Positive/negative tests for Custom HTTP connector
   - Allow path: Custom connector invokes Key Vault secrets endpoint inside Managed Environment (200)
   - Deny path: Same call from non-linked environment fails (403)
   - Workstation: curl to Key Vault returns 403
   - Message: Custom connectors subject to same allow/deny validation rules

**Pattern:** Each test file emphasizes dual-path validation (allow inside Managed Environment, deny from workstation/non-linked environment)

---

## Findings & Filings

### 🟡 Network Validation Scope Gaps

**Findings:** Original `scripts/03-validate-network.sh` had three issues:
1. Used operator-box `dig` as proxy for Private Link DNS (weaker validation)
2. Public denial checks were too loose (`non-2xx` hiding difference between 403 and other failures)
3. Validation scope narrower than docs claim (per Morpheus findings)

**Fixes applied:** All three issues addressed in rewritten script

**Filed:** Referenced in Morpheus findings

### 🟡 Demo Script Clarity

**Findings:** `docs/demo-script.md` did not:
1. Clearly separate workstation denial probes from Managed Environment allow-path proof
2. Include dated runnable commands for repeatability
3. Emphasize the dual-path validation pattern

**Fixes applied:** All three addressed in demo script rewrite

**Filed:** Referenced in Morpheus findings

### 🟡 Connector Test Coverage

**Finding:** Every connector guide lacks explicit negative-path tests and expected outputs

**Approach:** Neo prepared 4 merge-ready test-step files for Niobe to integrate (not directly edited, to preserve Niobe's ownership of connector prose)

**Filed:** 4 files in `.squad/decisions/inbox/` for Niobe merge pipeline

---

## Work Artifacts

**Files updated:**
- `scripts/03-validate-network.sh` (complete rewrite: A-record checks, zone-link checks, explicit deny outcomes, scope clarity)
- `docs/demo-script.md` (dated pre-demo commands, explicit deny tests, dual-path pattern emphasis)

**Decision files created / prepared for merge:** 4 neo-tests-*.md files

---

## Status

✅ **Sweep complete**  
Network validation now validates Azure-side Private Link setup + denial paths. Demo script emphasizes workstation denial proof vs. Managed Environment allow proof.

**Next steps:**
- Niobe: Integrate neo-tests-*.md files into connector docs
- Tank + Neo: Parameterize validation script RG lookup, add enterprise policy + DNS zone checks
