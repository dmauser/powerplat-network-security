# Orchestration Log: tank-5 & niobe-part4-planned
**Session timestamp:** 2026-05-21T23:26:00Z  
**Agents:** tank-5, niobe-part4-planned  
**Status:** Completed (tank-5), Planned/Blocked (niobe-part4-planned)

---

## Summary

**tank-5** successfully completed **West Flow Log Migration** to `rg-pbinet-dev-eastus` with platform constraint learning on NetworkWatcher singleton behavior.

**niobe-part4-planned** documented **Part 4 Dual-Region Function App** architectural handover as planned/blocked on MCAP subscription VM quota (zero quota for all App Service Plan SKUs).

---

## agent: tank-5

**Deliverable:** West flow log migrated from NetworkWatcherRG to `rg-pbinet-dev-eastus`

**Work performed:**
1. Completed flow log export from west region (VNet `vnet-pbinet-westus` → flow logs → Storage `stsecretwest`)
2. Identified platform constraint: Azure creates NetworkWatcher in `NetworkWatcherRG` per region by default; workaround requires `az resource move` to relocate watcher + child flow log while respecting per-region uniqueness
3. Applied `az resource move --destination-group rg-pbinet-dev-eastus --ids <watcher-id>` to consolidate west watcher into target RG
4. Verified flow logs operational post-migration

**Platform constraint learning:**  
NetworkWatcher is a singleton resource per region per subscription. Multiple `az network watcher configure -g <rg>` calls for same region return the existing watcher (in NetworkWatcherRG) without re-creating. Single-RG enforcement requires `az resource move` to consolidate watchers while maintaining region uniqueness.

**Commit:** 48391b0 (tank-west-flowlog-migrated)

---

## agent: niobe-part4-planned

**Deliverable:** Part 4 Dual-Region Function App documented as planned/blocked with unblock path

**Work performed:**
1. Documented Part 4 architectural foundation: east App Service Plan (eastus) + west App Service Plan (westus) with VNet integration to delegated subnets
2. Documented cross-region call path: west function → 10.20.2.X (west snet-funcapp) → peering to 10.10.X.X (east snet-funcapp) → east Key Vault
3. Identified blocker: MCAP internal subscription has zero VM quota for ALL App Service Plan SKUs (EP1, S1, P1v2, B1)
4. Documented unblock path: use Pay-As-You-Go subscription or request quota increase with MCAP platform team
5. Lift architectural handover to Scribe decisions.md for team visibility

**Architectural context:**  
Part 4 is the only deterministic validation path for Power Platform's non-deterministic VNet-injected runtime selection between eastus and westus delegated subnets. East/west function apps provide subnet-level tracing (source IP 10.10.2.X vs 10.20.2.X) that proves runtime locality. Cannot proceed until VM quota constraint resolved.

**Commit:** 8eaf13e (niobe-part4-planned-blocked-quota)

---

## Next Steps

- **East flow-log migration** in-flight in parallel (expected output: `tank-east-flowlog-migrated.md`)
- **VM quota resolution** for Part 4 (coordinated with MCAP platform team, expected ~2–3 business days)
- **Session records archival** for decisions.md when file reaches 20480 bytes (currently 20377 bytes; within threshold)
