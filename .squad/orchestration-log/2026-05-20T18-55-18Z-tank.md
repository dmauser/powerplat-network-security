# Orchestration Log — Tank

**Agent:** Tank (Power Platform Ops)  
**Timestamp:** 2026-05-20T18:55:18Z  
**Request:** Squad repo spot review, errors & improvements  
**Mode:** Parallel fan-out (5 agents)

---

## Scope

**Primary:** Script review (00-prereqs, 02-configure-pp-vnet, 05-cleanup), Power Platform module pinning, idempotency, docs alignment  
**Files reviewed:** scripts/00-prereqs.sh, scripts/02-configure-pp-vnet.ps1, scripts/05-cleanup.sh, docs/managed-environment-setup.md

---

## Work Completed

### 1. Prereqs Script Hardened (scripts/00-prereqs.sh)

**Fixes:**
- ✅ Added explicit tool version gates (az CLI, bicep, pwsh minimum versions)
- ✅ Added `ERR` trap for proper error handling
- ✅ Normalized line endings to LF (CRLF issue flagged by Trinity)
- ✅ Registered exactly Learn-documented Power Platform VNet prerequisites:
  - `Microsoft.PowerPlatform`
  - `Microsoft.Sql`
  - `Microsoft.KeyVault`
  - `Microsoft.Storage`
  - `Microsoft.Network`
  - `enterprisePoliciesPreview` feature flag

**Decision filed:** Whether infra-only providers like `Microsoft.ManagedIdentity` should be registered here or elsewhere (see tank-trinity-managedidentity-provider-ownership.md)

### 2. PP VNet Link Script Hardened (scripts/02-configure-pp-vnet.ps1)

**Fixes:**
- ✅ Pinned `Microsoft.PowerPlatform.EnterprisePolicies` module to v0.17.0
- ✅ Made script re-run safe: same policy = no-op path (idempotent)
- ✅ Added region validation for US geography

**Consequence:** Script can now be safely re-executed without creating duplicate policy linkages

### 3. Cleanup Script Fixed (scripts/05-cleanup.sh)

**Fixes:**
- ✅ Removed hard-coded resource group reference (`rg-pbinet-dev`)
- ✅ Made deletion targets parameterized

### 4. Documentation Alignment (docs/managed-environment-setup.md)

**Updated:**
- Aligned docs to reflect new version gates
- Clarified PP module pinning
- Noted idempotency of re-link operation

**Follow-up decision filed:** Whether broader docs (deployment-guide, troubleshooting, README) should also explicitly call out version gates, `enterprisePoliciesPreview` wording, and re-run safety (see tank-niobe-doc-sync-prereqs-and-link-noop.md)

---

## Findings & Filings

### 🟡 Validation Script Narrowness (with Neo)

**Scope:** `scripts/03-validate-network.sh`

**Finding:** Hard-codes resource group, validates only public reachability/DNS; does NOT validate enterprise policy subnet references, dual delegated subnet references, or private DNS zone links (docs claim full validation)

**Filed:** Included in Morpheus findings  
**Owner collaboration:** Tank + Neo

**Suggested follow-up:**
- Parameterize RG lookup from `.azure/last-deploy-outputs.json`
- Add enterprise policy, DNS zone, private endpoint checks

### 🟡 Demo Artifact Provisioning (with Niobe)

**Scope:** Deployment artifacts vs. docs assumptions

**Finding:** Bicep creates Key Vault secrets, but NOT SQL objects (`dbo.Sales`) or blob content (`demo/hello.txt`). Docs assume these exist.

**Filed:** Included in Morpheus findings  
**Owner collaboration:** Tank + Niobe

**Suggested follow-up:**
- Automate prep in post-deploy scripts, OR
- Clearly mark as manual pre-demo preparation in docs

### 🟡 Broader Doc Sync Decision

**Scope:** deployment-guide.md, troubleshooting.md, README.md

**Decision needed:** Explicitly call out:
1. Version gates in `00-prereqs.sh`
2. `enterprisePoliciesPreview` registration wording
3. `02-configure-pp-vnet.ps1` re-run safety

**Filed:** `.squad/decisions/inbox/tank-niobe-doc-sync-prereqs-and-link-noop.md`

### 🟡 Provider Registration Ownership (with Trinity)

**Scope:** `scripts/01-deploy.sh` vs. infra-only provider registration

**Finding:** `Microsoft.ManagedIdentity` and other non-PP/SQL/KV/Storage/Network providers must be registered, but ownership is now outside Tank's prereq scope

**Decision needed:** Should `scripts/01-deploy.sh` self-register, or should requirement be documented elsewhere?

**Filed:** `.squad/decisions/inbox/tank-trinity-managedidentity-provider-ownership.md`

---

## Work Artifacts

**Files updated:**
- `scripts/00-prereqs.sh` (version gates, ERR trap, LF normalization)
- `scripts/02-configure-pp-vnet.ps1` (module pinning to v0.17.0, idempotency path, region validation)
- `scripts/05-cleanup.sh` (parameterized RG reference)
- `docs/managed-environment-setup.md` (alignment to script updates)

**Decision files created:** 2

---

## Status

✅ **Sweep complete**  
All scripts now pass syntax validation and idempotency checks.

**Next steps:**
- Broader doc alignment (Niobe + Tank)
- Provider registration ownership decision (Trinity + Tank)
- Validation script parameterization (Neo + Tank)
- Demo artifact provisioning decision (Niobe + Tank)
